Allow overriding of qmake step with cygqmake4

--- origsrc/smplayer-0.8.6/Makefile	2013-11-13 19:12:19.592497100 -0600
+++ src/smplayer-0.8.6/Makefile	2013-11-13 19:22:13.717479100 -0600
@@ -39,11 +39,14 @@ QMAKE_OPTS=DEFINES+=KDE_SUPPORT INCLUDEP
 
 endif
 
-src/smplayer:
-	./get_svn_revision.sh
-	+cd src && $(QMAKE) $(QMAKE_OPTS) && $(DEFS) make
+src/smplayer: src/Makefile
+	cd src && $(DEFS) make
 	cd src && $(LRELEASE) smplayer.pro
 
+src/Makefile:
+	./get_svn_revision.sh
+	cd src && $(QMAKE) $(QMAKE_OPTS)
+
 clean:
 	if [ -f src/Makefile ]; then cd src && make distclean; fi
 	-rm src/translations/smplayer_*.qm
